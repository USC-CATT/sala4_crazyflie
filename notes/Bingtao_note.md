# Mar. 03 2026

- Edit `pid_pwm_position.py` and `plotter_test.py`. Now the plot shows top view, x, y, and z in terms of second. 
- Changed `PID_VEL_THRUST_BASE` from 30000 to 35000 and `PID_POS_Z_KP` from 2 to 3
- Added `augmented_pid_pwm_position.py`. It has an unexpected swing in the x direction if X/Y agumentation is turned on. 
